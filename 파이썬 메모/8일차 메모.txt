지금 당장 집중
# 함수 기본 개념/사용자 입력 개념
# 키워드/키오스크/계산기
#
# 시퀀스 프로그램
# 시퀀스 다이어그램
# 프로세스
# 시퀀스 다이어그램
# 실시간 기능 명세/시스템 복잡한 시나리오 쉽게 /객체 교환메세지/설계(시퀀스 다이어그램)중요
# 설계 주체(추상화(세분화)에 따라서 변화),처음엔 크게 분류/모듈화/ATM,유저,뱅크서버

유저 중심 등의 기능
어디 포함되어 있는 가
시퀀스 전에 흐름도(flow chart)
시작&종료 타원
직사각형(프로세스)>수행 작업 및 단계
마름모(결정)
평행
입력/출력
플로우차트
소프트개발
고객지원 플로우차트

1. 주문하기
if x==1 1.포장/2.매장
eslf
카테고리 유지>누르면 오른쪽 부분에 세부메뉴(종류)버거 선택
메뉴 세트/취소

시작>주문>포장>버거>세트>사이즈 일반>사이드메뉴(취소)>수량선택/장바구니/옵션 수정

시간 제안 datetime 
사이즈 선택


처음으로 완전히 주문허기 버튼 화면으로
이전 단계 바로 전 단계 화면으로(세트 구성하는 과정처럼 연계되는 과정에서 나타나는 버튼) 
취소: 메인 카테고리 화면으로

처음으로 버튼/취소 버튼이 메인
도움기능은 X

while True 
이전 단계로 묶어주는 것은 continue


# if 구분>값 확인>함수 계산>출력>(그 외 작성 안되게)

while True:

    number= input("입력")
    l = ["+","-","/","*"]
    # number=int(number)

    if number==str(l):
        print(number)
    else:
        print(type(number))
        print(number)
        if number == int(number):
            print("c")
            print(number)

# # 학생 딕셔너리에는 5명의 학생 정보다 있다
# # 학생 정보의 종류는 이름,학년,전공,전공종류는 4가지(국,영,수,검)
#
# bbb=[]
# aaa=[2,3,4,5,6]
# bbb.append(aaa)
# print(aaa)
# print(bbb)

while True: #반복문
    x=input("1.주문하기")
    if x=="1":
        x2=input("1.포장 2.매장")
        if x2=="2":
            print("모든 카테고리 출력 1~8")
            print("버거의 소분류 모두 출력(기본값 전체) 9~14")
            print("모든 버거의 종류 15~22")
            print("처음으로 23")
            y=input("1~23번 중 어떤거")
            if y==23:
                continue #반복문 처음으로